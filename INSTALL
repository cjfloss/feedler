Dependencies
------------
 - libgtk-3.0-dev
 - libwebkitgtk-3.0-dev
 - libgranite
 - sqlheavy-0.1
 - libsoup-2.4
 - libxml-2.0
 - libnotify
 - libunity
 - gee-1.0
 - indicate-0.7

Testing
------------
$ mkdir build # in feedler folder
$ cd build/
$ cmake -DCMAKE_INSTALL_PREFIX=/usr ..
$ make
$ ./feedler # service cannot work without install

Installation
------------
$ sudo make install # after commands from 'Testing'

Have a nice day with Feedler;)
