Dependencies
------------
 - libgtk-3.0-dev
 - libwebkitgtk-3.0-dev
 - libgranite
 - sqlheavy-0.1
 - libsoup-2.4
 - libxml-2.0
 - libnotify
 - gee-1.0

Testing
------------
$ cd src/service # in feedler folder
$ mkdir build
$ cd build/
$ cmake -DCMAKE_INSTALL_PREFIX=/usr ..
$ make
$ ./feedler-service

$ mkdir build # in feedler folder
$ cd build/
$ cmake -DCMAKE_INSTALL_PREFIX=/usr ..
$ make
$ ./feedler

Installation
------------
$ sudo make install # after commands from 'Testing'

Have a nice day with Feedler;)
